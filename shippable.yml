language: none

services:

build:
  pre_ci:
  pre_ci_boot:
    image_name: sireum/v3
    image_tag: latest
    pull: true
    options: "-e HOME=/root"
  ci:
    - sbt "project scalac-plugin" publishLocal
    - git clone https://github.com/sireum/scalac-plugin-test.git sireum-scalac-plugin-test
    - cd sireum-scalac-plugin-test
    - sbt test
  post_ci:
  on_success:
  on_failure:
  push:
  cache: true
  cache_dir_list:
    - $HOME/.sbt
